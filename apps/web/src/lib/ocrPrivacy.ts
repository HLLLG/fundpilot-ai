// 本地 PaddleOCR 兜底已删除，原来那句「local 模式由应用服务端本地 OCR 处理，不转发给
// 第三方 OCR」不再成立，必须如实改写：现在截图一定会经服务端转发到阿里云百炼做文字识别。
export const OCR_PRIVACY_COPY = {
  shortLabel: "截图经服务端送云端识别",
  trustTitle: "截图传输与识别路径透明可控",
  trustDescription:
    "截图先发送到本应用服务端，再由服务端转发至阿里云百炼完成文字识别（仅做图片转文字）。识别文本按图片哈希缓存，重复上传同一张截图不再外发。投研模型只接收你确认后的结构化持仓，不接收原始截图。不希望截图外传时，请改用手动输入持仓。",
  uploadNotice:
    "截图由本应用服务端转发至阿里云百炼识别文字；投研模型仅接收结构化持仓。不想外传可改用手动输入。",
} as const;
